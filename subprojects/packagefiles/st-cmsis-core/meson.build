# Not fully supported!
project('st-cmsis-core', 'c', version : '5.9.0')
root = 'cmsis-core-5.9.0'

st_cmsis_core_dep = declare_dependency(
  include_directories: root / 'Include',
  version: meson.project_version(),
)
